@model List<PIZZERIA.Models.Consultas>

@{
    ViewData["Title"] = "Consultas Encontradas";
    var count = 1;
}
<h1><center>@ViewData["Title"]</center></h1>
<form asp-controller="Consultas" asp-action="BuscarConsultas" method="GET">
    <div class="form-group row">
        <label class="control-label">Ingresar Usuario</label>
        <input type="email" name="usuario" class="form-control">
    </div>
</form><br>
<table class="table table-hover">
    <thead class="thead-dark">
        <tr>
            <th scope="col">
                №
            </th>
            <th scope="col">
                Usuario
            </th>
            <th scope="col">
                Correo Electronico
            </th>
            <th>
                Comentario
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
           <th scope="row">
                @count
            </th>
            <td>
                @item.UserID
            </td>
            <td>
                @item.Email
            </td>                
            <td>
                @item.Comentario
            </td>
        </tr>
        count++;
}
    </tbody>
</table>
